<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Adimeo File Manager</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link {% if currentPage == 'files' %}active{% endif %}" href="/">Files</a>
            </li>
            {% if user.isAdmin %}
            <li class="nav-item">
                <a class="nav-link {% if currentPage == 'groups' %}active{% endif %}" href="{{ path('groups') }}">Groups</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if currentPage == 'users' %}active{% endif %}" href="{{ path('users') }}">Users</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
